{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Address Field Types plugin for Craft CMS
 *
 * Country Field Settings
 *
 * @author    Imarc
 * @copyright Copyright (c) 2021 Imarc
 * @link      https://www.imarc.com
 * @package   AddressFieldTypes
 * @since     1.0.0
 */
#}

{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("imarc\\addressfieldtypes\\assetbundles\\addressfieldtypes\\AddressFieldTypesAsset") %}

{# {% set tLabel =  'Default Country'|t %}
{% set tAlpha1 =  'Default Country'|t %}
{% set tAlpha2 =  'Default Country'|t %}
{% set tNumeric =  'Default Country'|t %}

{{ forms.select({
    label: tLabel,
    id: 'valueFormat',
    name: 'valueFormat',
    value: field['valueFormat'],
    options:[
        {value:'alpha2',label:tAlpha1},
        {value:'alpha3',label:tAlpha2},
        {value:'numeric',label:tNumeric}
    ]})
}} #}

{% set tLabel =  'Default Country'|t %}
{% set tInstructions = 'Enter the ISO 3166-1 code (alpha-2, alpha-3, or numeric) for the default country'|t %}

{{ forms.textField({
    label:tLabel,
    instructions: tInstructions,
    id: 'default',
    name: 'default',
    value: field['default']})
}}

{% set tLabel =  'Disabled Countries'|t %}
{% set tInstructions = 'Enter a comma separated list of the ISO 3166-1 codes (alpha-2, alpha-3, or numeric) for any disabled countries'|t %}

{{ forms.textField({
    label:tLabel,
    instructions: tInstructions,
    id: 'disabled',
    name: 'disabled',
    value: field['disabled']})
}}
